<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="it.giuseppeliccardo.ionutrizionista" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/Theme.AppCompat.Light.DarkActionBar" >

        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".CalcoloValoriEnergeticiActivityOLD"
            android:label="@string/title_activity_calcolo_valori_energetici"
            android:parentActivityName=".MainActivity"
            android:screenOrientation="portrait" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="it.giuseppeliccardo.ionutrizionista.MainActivity" />
        </activity>

        <activity
            android:name=".CalcoloValoriEnergeticiActivity"
            android:label="@string/title_activity_calcolo_valori_energetici_v2"
            android:parentActivityName=".MainActivity"
            android:screenOrientation="portrait" >
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="it.giuseppeliccardo.ionutrizionista.MainActivity" />
        </activity>

    </application>

</manifest>
